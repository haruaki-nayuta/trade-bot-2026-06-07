"""コンフルエンス平均回帰 v2 = チャンピオン + 効率比(ER)エントリーフィルタ。本リポジトリの新チャンピオン。

v1(confluence_meanrev)の唯一にして既知の弱点は「戻らずに長期保有で失血する塩漬けトレード」だった
(corr(保有,損益)=-0.85、ワースト10%が総損失の72.5%)。出口を切るのは平均回帰では逆効果と実証済み
(損切り/時間ストップは全指標悪化)。残る打ち手=エントリー側を多角検証(Workflow: 5レンズ診断→
仮説→IS/OOS+高原+敵対検証)した結果、**汎用・非カーブフィットで効くフィルタは1つだけ**と判明した:

  ★ Kaufman 効率比 ER(40) ≤ 0.55 のときだけ建玉する。
    ER = |Δ40本| / Σ|Δ1本| ∈ [0,1]。1に近い=一直線の強トレンド、0に近い=往復の多いレンジ。
    平均回帰が必要とするのは「行き過ぎたが地合いはレンジ(ER低)」。一直線トレンド(ER高)への
    逆張りは"落ちるナイフ"=塩漬けの温床。これを入る前に外す。

検証(19対象=7メジャー+12クロス, AUDJPY除外, H4, size=value):
  * フル11年: trades 1232→1214(-1.5%) / total_pnl 18109→19072(+5.3%) / PF 1.638→1.708 /
    月次Sharpe 1.694→1.862 / 最大DD -0.71%→-0.61% / 11年連続プラス維持
  * OOS(2022-): total_pnl +10.2% / PF 1.754→1.918 / Sharpe 1.853→2.172(改善幅がISより大=過剰最適化でない)
  * 仕組み: 除外された23トレードは純損失コホート(合計-1046, 勝率43.5%, 平均36本保有=塩漬け帯)。
    良いトレードは残るのでリターンとリスクが同時改善。
  * 高原性: er_max∈[0.40,0.60] / er_win∈[20,60] のいずれもベースライン超(単一点の偶然でない)。
  * 棄却された罠: 損切り/時間ストップ(出口)、slow_z_cap・atr_cap・slope_max・adx(エントリー)。

正直な限界: これはコホート単位のリスク低減であって、テール上限ではない。単発ワースト(NZDCAD
ショート -7.55%, 140本)はER中位で入っており、どのしきい値でも除外されない=塩漬けの"発生そのもの"を
エントリーで根絶することはカーブフィット無しには不可能(=平均回帰エッジの不可避コスト)。

詳細は reports/08(本検証)/ champion-entry-filter Workflow。先読みなし・close のみ=クロスにも適用可。
"""

from __future__ import annotations

from .confluence_meanrev_filtered import generate_signals  # noqa: F401 (CLI が参照)

# 本番パラメータ(v1 production_spec + ER フィルタ)
PARAMS = {"window": 50, "entry_z": 2.0, "exit_z": 0.5, "rsi_p": 14, "rsi_low": 35,
          "rsi_high": 65, "vol_win": 100, "vol_pct": 0.70, "slow_win": 250, "slow_z": 1.75,
          "er_win": 40, "er_max": 0.55,
          # 検証で無効と確認した他フィルタは OFF 固定
          "adx_p": 14, "adx_max": None, "slow_z_cap": None,
          "atr_p": 14, "atr_cap": None, "slope_win": 10, "slope_max": None}

# 探索は ER の高原のみ(チャンピオン中核は再最適化しない=カーブフィット回避)
PARAM_GRID = {
    "er_win": [30, 40, 50],
    "er_max": [0.45, 0.50, 0.55, 0.60, 1.01],  # 1.01 = フィルタOFF(=v1)
}
